services:
  web:
    build:
      context: .
    environment:
      GUARDIAN_API_KEY: ${GUARDIAN_API_KEY:?GUARDIAN_API_KEY is required}
    ports:
      - '127.0.0.1:${APP_PORT:-8080}:80'
    restart: unless-stopped
