services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://jsonplaceholder.typicode.com}
    ports:
      - '${APP_PORT:-8080}:80'
    restart: unless-stopped
