#!/bin/sh
set -eu

# envsubst вставляет значение прямо в конфигурацию Nginx; спецсимволы здесь недопустимы.
case "${GUARDIAN_API_KEY:-}" in
  ''|*[!a-zA-Z0-9_-]*)
    echo 'GUARDIAN_API_KEY должен быть задан и содержать только буквы, цифры, дефис или подчёркивание.' >&2
    exit 1
    ;;
esac
